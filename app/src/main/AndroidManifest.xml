<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for location and internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ev_syatem">

        <!-- SplashActivity is now the launcher -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LoginActivity (navigated after splash) -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <!-- RegisterActivity (navigated from login) -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <!-- HomeActivity (home screen after successful registration/login) -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <!-- ProfileActivity (user profile screen) -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false" />

        <!-- StationMapActivity (map showing charging stations) -->
        <activity
            android:name=".StationMapActivity"
            android:exported="false" />

        <!-- MainActivity (your dashboard / home after login) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

    </application>
</manifest>
